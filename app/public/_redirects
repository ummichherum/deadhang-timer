# Netlify SPA redirects for Deadhang Timer
# All non-file requests should go to index.html for client-side routing

# Static assets
/assets/*  /assets/:splat  200

# Audio files
/audio/*   /audio/:splat   200

# Service Worker
/sw.js     /sw.js          200

# PWA Manifest
/manifest.webmanifest  /manifest.webmanifest  200

# Icons and favicon
/vite.svg              /vite.svg              200
/favicon.ico           /favicon.ico           200

# API routes (if any future API endpoints)
/api/*     /api/:splat    404

# All other routes → SPA
/*         /index.html    200 